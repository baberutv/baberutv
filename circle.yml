general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 6.9.1

dependencies:
  cache_directories:
    - ~/.yarn-cache
  override:
    - npm install -g yarn
    - yarn

test:
  override:
    - yarn test

deployment:
  publish:
    tag: /v\d+(\.\d+)*/
    commands:
      - ./scripts/deploy.sh
